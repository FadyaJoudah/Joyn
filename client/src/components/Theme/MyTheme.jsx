import { createTheme, ThemeProvider } from "@material-ui/core/styles";

export const myTheme = createTheme({
  palette: {
    primary: {
      main: "#607d8b",
    },
    secondary: {
      main: "#ffc107",
    },
  },
});
